整个页面最左边是各个平台的按钮，点击可以快速切换，并且希望可以有整个页面是由按钮展开的效果，这一排按钮的最上方是我们软件的logo,点击可以回到主页面，我们的主页面可以重新设计一下，先不着急，最下方的按钮就是现在的设置按钮
最上方的右边是自己的头像和昵称，点击可以快速切换到自己的主页，左边是平台的logo

我们现在理清目前需要展示的信息：
单作品：分为视频，图集和文章
页面布局：
1链接输入框（检测到链接自动解析，所以不需要解析按钮），需要一个清除的按钮用来清空输入框
2保存路径输入框（默认为设置页面的路径），浏览按钮
3作者信息，包括头像、昵称、签名、作品数、关注数、粉丝数，以及该作品的简介，并且在这一行最后有一个下载选中按钮，还有取消按钮（用来暂停加载）
4是预览窗口，若为视频：视频封面。若为文章，显示文章。若为图集：展示所有预览图，并且可以单独勾选需要下载的图片，就和旧版本一样，支持滑动勾选
5左边是日志，右边是已经下载的目录列表，目录列表支持点击进入，并且可以删除目录

他人主页：
页面布局：
1链接输入框（检测到链接自动解析，所以不需要解析按钮），需要一个清除的按钮用来清空输入框
2保存路径输入框（默认为设置页面的路径），浏览按钮
3作者信息，包括头像、昵称、签名、作品数、关注数、粉丝数，并且在这一行最后有一个下载选中按钮，还有取消按钮（用来暂停加载）
4是预览窗口，保持和旧版本的勾选弹窗一样的布局，但是我们这个是在一个页面里显示，旁边有滑动条可以向下滚动，每个作品用一行显示，并且可以单独勾选需要下载的图片，就和旧版本一样，支持滑动勾选
5左边是日志，右边是已经下载的目录列表，目录列表支持点击进入，并且可以删除目录

最后目录列表和下载路径的输入框的显示需要根据链接的类型动态变换加载
